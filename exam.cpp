#include <iostream>
#include <fstream>
#include <string>
using namespace std;

int main()
{
    // string my[6];
    // ifstream myfile;
    // string reader;
    // myfile.open("dum.txt", ios::in);

    // if (myfile.is_open())
    // {
    //     getline(myfile, reader);
    //     cout << reader;
    //     for (int i = 0, j = 0; i < 5 && j < reader.length(); i++, j++)
    //     {
    //         // Use single quotes to compare with the character ','
    //         while (reader[j] != ',' && j < reader.length())
    //         {
    //             my[i] += reader[j];
    //             j++;
    //         }

    //         cout << my[i] << endl;
    //     }

    //     myfile.close();
    // }
    // else
    // {
    //     cout << "Unable to open file" << endl;
    // }

    // return 0;
}
